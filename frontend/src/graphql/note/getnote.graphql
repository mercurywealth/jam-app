query GetNote($id: String!) {
    note(id: $id){
        id,
        title,
        description,
        owner {
            id,
            email,
            firstName,
            lastName
        }
    }
}